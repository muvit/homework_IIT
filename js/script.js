/* 
   Файл управления функционалом страницы
*/

// Функция для возврата на предыдущую страницу
function goBack() {
    window.history.back();
}

// Функция для перехода на следующую страницу
function goForward() {
    window.history.forward();
}